# Модуль 4. Урок 26. Связь One-To-One (Один к одному)

В этом уроке мы разберём одну из трёх основных связей между таблицами — **One-To-One (один к одному)**.

Эта связь часто используется для **расширения модели** дополнительной информацией, не перегружая основную таблицу.

---

## Что значит «Один к одному»

В реляционной базе данных связь _один к одному_ означает, что **каждая запись одной таблицы соответствует ровно одной записи другой таблицы**.

Примеры таких связей:

- У каждого пользователя есть **один профиль** с дополнительной информацией.
- У каждого фильма есть **одна страница с детальной статистикой просмотров**.
- У каждого режиссёра может быть **одна персональная биография**.

> 💡 Проще говоря: если одна таблица — это "основная сущность", а другая — "дополнение", то связь `OneToOne` идеально подходит.

---

## Пример из проекта Cinemahub

Представим, что у нас есть модель `Director` (режиссёр), и теперь мы хотим хранить **подробную биографию режиссёра** — не просто дату рождения, а полное описание, место рождения, награды и т.д.

Логично вынести эту информацию в отдельную таблицу `DirectorProfile`, чтобы не перегружать основную таблицу `Director`.

---

## 🧩 Создаём модели

Откройте файл `cinemahub/models.py` и добавьте две модели:

```python
from django.db import models

class Director(models.Model):
    name = models.CharField(max_length=150)
    age = models.PositiveIntegerField(null=True, blank=True)
    country = models.CharField(max_length=100, null=True, blank=True)

    def __str__(self):
        return self.name


class DirectorProfile(models.Model):
    director = models.OneToOneField(
        Director,
        on_delete=models.CASCADE,
        related_name='profile'
    )
    biography = models.TextField(blank=True)
    awards = models.CharField(max_length=255, blank=True)
    birthplace = models.CharField(max_length=150, blank=True)

    def __str__(self):
        return f"Профиль режиссёра: {self.director.name}"
```

### Разбираем подробно:

- `OneToOneField` связывает `DirectorProfile` с `Director`.
- Аргумент `on_delete=models.CASCADE` означает, что если режиссёр удаляется — его профиль тоже будет удалён.
- Параметр `related_name='profile'` позволяет получить профиль через `director.profile`.
- `DirectorProfile` не может существовать без `Director`, потому что это _одна сущность, разбитая на две таблицы_.

---

## Создаём и применяем миграции

После добавления моделей выполним команды:

```bash
python manage.py makemigrations
python manage.py migrate
```

Если всё успешно — Django создаст таблицы `cinemahub_director` и `cinemahub_directorprofile`.

---

## Работаем с данными через Django Shell

Теперь проверим, как это работает на практике.

Откроем shell:

```bash
python manage.py shell
```

### 1. Создаём несколько режиссёров:

```python
from cinemahub.models import Director, DirectorProfile

d1 = Director.objects.create(name="Кристофер Нолан", age=53, country="Великобритания")
d2 = Director.objects.create(name="Квентин Тарантино", age=61, country="США")
```

### 2. Добавляем профили:

```python
p1 = DirectorProfile.objects.create(
    director=d1,
    biography="Известен своими сложными сценариями и нелинейным монтажом.",
    awards="Оскар, BAFTA, Золотой глобус",
    birthplace="Лондон, Великобритания"
)

p2 = DirectorProfile.objects.create(
    director=d2,
    biography="Сочетает черный юмор и стиль насилия. Любит отсылки к поп-культуре.",
    awards="Оскар, Каннский фестиваль",
    birthplace="Ноксвилл, США"
)
```

### 3. Проверим связь в действии:

```python
d1.profile.biography
# "Известен своими сложными сценариями и нелинейным монтажом."

p2.director.name
# "Квентин Тарантино"
```

Мы можем обращаться **в обе стороны**:

- `director.profile` — получить профиль.
- `profile.director` — получить режиссёра.

---

## Попробуем нарушить уникальность

Попробуем создать ещё один профиль для того же режиссёра:

```python
DirectorProfile.objects.create(
    director=d1,
    biography="Ещё один профиль"
)
```

Результат:

```plaintext
django.db.utils.IntegrityError: UNIQUE constraint failed: cinemahub_directorprofile.director_id
```

Ошибка говорит, что у режиссёра может быть **только один профиль**.
Именно это и есть принцип One-To-One: уникальная связь между записями.

---

## Изменение и удаление

Мы можем обновлять или удалять данные точно так же, как в обычных моделях:

```python
d1.profile.awards = "Оскар, BAFTA"
d1.profile.save()
```

Если удалить режиссёра:

```python
d2.delete()
```

его профиль тоже будет автоматически удалён (из-за `on_delete=models.CASCADE`).

---

## Проверка результата через Django Admin

1. Зарегистрируйте обе модели в `cinemahub/admin.py`:

```python
from django.contrib import admin
from .models import Director, DirectorProfile

admin.site.register(Director)
admin.site.register(DirectorProfile)
```

2. Запустите сервер и откройте `/admin/`.
3. Добавьте режиссёра, затем создайте для него профиль.
4. Попробуйте создать второй профиль для того же режиссёра — получите ошибку.
5. Убедитесь, что при удалении режиссёра его профиль тоже исчезает.

---

## Полезные советы

- Если нужно хранить дополнительные данные о пользователе, **лучше использовать OneToOneField**, а не добавлять десятки новых полей в модель `User`.
- В связях OneToOne можно использовать **`on_delete=models.SET_NULL`**, если хотите сохранять профиль даже при удалении основной записи.
- Проверяйте, что у связанной модели не создаются дубликаты — это частая ошибка при ручном добавлении данных.

---

## Схема зависимости

```
Director ────────▶ DirectorProfile
     |                  |
     | OneToOneField    |
     ▼                  ▼
  (одна запись)     (одна запись)
```

---

## Практические задания

### Задание 1

Создайте модель `MovieDetails`, связанную с моделью `Movie` (один к одному).
В `MovieDetails` добавьте поля:

- `duration` (в минутах),
- `budget`,
- `box_office`,
- `description`.

Проверьте в shell, что каждому фильму можно создать только **одну** запись `MovieDetails`.

---

### Задание 2

Создайте связь `OneToOne` между пользователем (`User`) и моделью `Profile`.
Пусть в `Profile` хранится дата рождения и город пользователя.

---

### Задание 3

Попробуйте изменить поведение удаления с `on_delete=models.CASCADE` на `on_delete=models.SET_NULL`.
Проверьте, что произойдёт при удалении объекта.

---

## Вопросы

1. Что означает связь **One-To-One** в Django ORM?
2. В чём отличие `OneToOneField` от `ForeignKey`?
3. Что делает параметр `related_name`?
4. Что произойдёт при удалении режиссёра, если `on_delete=models.CASCADE`?
5. Почему возникает ошибка `UNIQUE constraint failed`?
6. Можно ли через `DirectorProfile` получить имя режиссёра? Каким образом?
7. Можно ли одному режиссёру создать несколько профилей? Почему?
8. Как можно изменить поведение при удалении основной записи, не теряя связанных данных?
9. В каких ситуациях One-To-One подходит лучше, чем ForeignKey?
10. Как проверить связь One-To-One в Django admin?

---

[Предыдущий урок](lesson25.md) | [Следующий урок](lesson27.md)
