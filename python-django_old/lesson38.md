# Модуль 6. Урок 38. Настраиваем внешний вид админ-панели Django

Админ-панель Django — это один из самых недооценённых инструментов. В неё редко заглядывают студенты, а зря: почти любой реальный проект использует её как внутренний интерфейс для менеджеров, редакторов контента или даже аналитиков.

В этом уроке мы научимся **настраивать внешний вид админки**, переопределять её шаблоны и собирать свою собственную стильную панель управления. После прохождения урока ты сможешь не только изменить внешний вид, но и подготовить админку под бренд проекта.

---

## 1. Для чего переопределять внешний вид админ-панели?

По умолчанию Django даёт нам довольно строгий, универсальный интерфейс. Он удобный, но абсолютно безликий.

В проекте **cinemahub** (как и в любом коммерческом проекте) часто нужно:

- добавить брендирование — логотип, название проекта;
- изменить цвета, чтобы панель выглядела как часть продукта;
- улучшить удобство администратора;
- скрыть элементы, которые не нужны контент-менеджерам;
- добавить собственные стили или JS-скрипты.

Все это делается через **переопределение шаблонов и подключение статических файлов**.

---

## 2. Как устроены шаблоны админ-панели

Django использует шаблоны, расположенные внутри пакета:

```
django/contrib/admin/templates/admin/
```

Основные файлы:

| Файл               | За что отвечает                                   |
| ------------------ | ------------------------------------------------- |
| **base.html**      | базовая структура всех страниц админки            |
| **base_site.html** | внешний вид главной страницы (логотип, заголовок) |
| **index.html**     | стартовая страница админки                        |
| **app_list.html**  | список моделей внутри приложения                  |

Мы можем **переопределить любой из этих шаблонов**, поместив файл с таким же названием в директорию:

```
templates/admin/
```

Когда Django видит там шаблон — он заменяет стандартный.

---

## 3. Создаём структуру шаблонов в проекте cinemahub

Обычно шаблоны админки добавляют в корневой `templates/`.

Создаём:

```
cinemahub/
│
├── templates/
│   ├── admin/
│   │   ├── base_site.html
│   │   ├── base.html   (опционально)
│   │   └── index.html  (опционально)
```

Если этой папки нет — создаём вручную.

Чтобы Django видел шаблоны, в settings.py должен быть прописан путь:

```python
TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [BASE_DIR / "templates"],
        ...
    }
]
```

---

## 4. Переопределяем base_site.html — добавляем бренд cinemahub

Создаём файл:

```
templates/admin/base_site.html
```

Вставляем:

```html
{% extends "admin/base.html" %} {% load static %} {% block title %}Cinemahub —
Панель управления{% endblock %} {% block branding %}
<h1 id="site-name">
  <img
    src="{% static 'img/cinemahub-logo.svg' %}"
    alt="Logo"
    style="height:40px; vertical-align:middle;"
  />
  <span style="margin-left:10px;">Cinemahub Admin</span>
</h1>
{% endblock %} {% block extrastyle %}
<link rel="stylesheet" href="{% static 'css/admin/custom-admin.css' %}" />
{% endblock %}
```

### Что мы сделали:

- расширили стандартный шаблон `admin/base.html`;
- добавили логотип;
- указали название проекта;
- подключили кастомный CSS.

### Проверка

Запускаем сервер:

```
python manage.py runserver
```

Открываем [http://127.0.0.1:8000/admin/](http://127.0.0.1:8000/admin/)

Теперь заголовок и логотип должны измениться.

Если нет — почти всегда проблема в неправильном пути в `TEMPLATES['DIRS']`.

---

## 5. Подключаем собственные стили

Создаём папку статических файлов:

```
static/
├── css/
│   ├── admin/
│   │   ├── custom-admin.css
├── img/
    ├── cinemahub-logo.svg
```

В файле:

```
static/css/admin/custom-admin.css
```

Добавляем стили:

```css
/* Верхняя панель */
#header {
  background-color: #1f1f1f;
  color: #fff;
}

/* Заголовки модулей */
.module caption,
.module h2 {
  background-color: #2e2e2e;
  color: #fff;
}

/* Хлебные крошки */
div.breadcrumbs {
  background: #3a3a3a;
  color: #fff;
}

/* Кнопки */
.button,
input[type="submit"] {
  border-radius: 6px;
  padding: 6px 12px;
  font-weight: 600;
}
```

### Проверка

Обнови страницу админ-панели в браузере.

Если изменений нет — значит:

- не подключён `{% load static %}`;
- неправильный путь до CSS;
- забыли `STATICFILES_DIRS`.

---

## 6. Обязательно добавляем STATICFILES_DIRS в settings.py

Если у проекта нет папки `static/` в корне — Django не увидит файлы.

Вставляем в `settings.py`:

```python
STATICFILES_DIRS = [
    BASE_DIR / "static"
]
```

Если static/ лежит внутри приложения — этот блок не нужен.

Но для кастомизации админки обычно используют корневую папку.

---

## 7. Переопределяем главную страницу админки (index.html)

Давай сделаем стартовую страницу красивее.

Создаём файл:

```
templates/admin/index.html
```

Добавляем:

```html
{% extends "admin/base_site.html" %} {% load static %} {% block content %}
<div class="dashboard-welcome">
  <h1>Добро пожаловать в админ-панель Cinemahub</h1>
  <p>Здесь вы можете управлять фильмами, жанрами и актёрами.</p>
</div>

{{ block.super }} {% endblock %}
```

Добавим стили:

```css
.dashboard-welcome {
  padding: 20px;
  background: #212121;
  border-radius: 12px;
  margin-bottom: 20px;
  color: #fff;
}
```

### Проверка

Обновляем страницу `/admin/` — сверху появится приветственный блок.

---

## 8. Переопределяем базовый шаблон admin/base.html (необязательно)

Это самый мощный инструмент.

С ним можно:

- добавлять JS-скрипты,
- полностью менять структуру интерфейса,
- подключать шрифты.

Но это делают редко, только когда нужно переписать админку под бренд.

Пример:

```
templates/admin/base.html
```

```html
{% extends "admin/base.html" %} {% load static %} {% block extrahead %}
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link
  href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;700&display=swap"
  rel="stylesheet"
/>
<style>
  body,
  input,
  select,
  textarea {
    font-family: "Rubik", sans-serif;
  }
</style>
{% endblock %}
```

Теперь админка использует аккуратный современный шрифт.

---

## 9. Возможные ошибки и как их исправлять

| Ошибка                                | Причина                               | Решение                                |
| ------------------------------------- | ------------------------------------- | -------------------------------------- |
| Статические файлы не применяются      | Django не знает про static/           | Проверить `STATICFILES_DIRS`           |
| Изменения в шаблоне не видны          | Шаблон не переопределён               | Проверить `templates/admin/<название>` |
| 404 на логотип                        | неправильный путь к static            | проверить `{% static '...' %}`         |
| CSS загружается, но стили не работают | селекторы не совпадают с HTML админки | проверить структуру в инспекторе       |

---

## 10. Полный пример стилизованной админ-панели (готовый вариант)

* 📁 `templates/admin/base_site.html`
* 📁 `templates/admin/index.html`
* 📁 `static/css/admin/custom-admin.css`
* 📁 `static/img/cinemahub-logo.svg`

Стилизация включает:

- новый шрифт,
- темную тему,
- округлённые кнопки,
- новую цветовую палитру,
- приветственный блок.

Эта структура полностью рабочая.

---

## 11. Практические задания

1. Добавь в админ-панель текстовый футер: «Создано для проекта Cinemahub».

2. Сделай фон всей админ-панели светло-серым. Использовать `body` для стилизации.

3. Измени стиль кнопок так, чтобы они были зелёными при наведении.

### Сравнить решение

1. Добавь в админ-панель текстовый футер: «Создано для проекта Cinemahub».

```html
{% block footer %}
<div style="padding: 10px; text-align: center; opacity: .7;">
  Создано для проекта Cinemahub
</div>
{% endblock %}
```

2. Сделай фон всей админ-панели светло-серым.

```css
body {
  background: #f2f2f2 !important;
}
```

3. Измени стиль кнопок так, чтобы они были зелёными при наведении.

```css
.button:hover,
input[type="submit"]:hover {
  background-color: #28a745 !important;
  color: #fff !important;
}
```

---

# 12. Вопросы для закрепления

1. Как Django понимает, что нужно использовать кастомный шаблон admin-панели?
2. Какие файлы шаблонов админки можно переопределить?
3. Зачем нужен файл `base_site.html`?
4. Для чего используется блок `extrastyle`?
5. Что будет, если забыть добавить `STATICFILES_DIRS`?
6. Где должен лежать CSS-файл кастомной админки?
7. Как проверить, что шаблон подгрузился?
8. Какие ошибки чаще всего возникают при стилизации админки?
9. Можно ли изменить шрифты админ-панели? Каким способом?
10. Где физически находятся стандартные шаблоны админки Django?

---

[Предыдущий урок](lesson37.md) | [Следующий урок](lesson39.md)
