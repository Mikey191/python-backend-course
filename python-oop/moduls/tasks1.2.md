# Практические задачи к уроку 2

**«Классы и объекты. Атрибуты классов и объектов»**

---

## 🟢 Лёгкий уровень

### Задача 1

**Тренируем:**

* объявление класса
* атрибуты класса
* работу с пространством имён класса

**Условие:**
Объявите класс с именем `DataBase`, который бы хранил в себе следующую информацию:

* `pk`: 1
* `title`: `"Классы и объекты"`
* `author`: `"Сергей Балакирев"`
* `views`: 14356
* `comments`: 12

Имена атрибутов используйте точно такие же.

#### Решение:

```python
class DataBase:
    pk = 1
    title = "Классы и объекты"
    author = "Сергей Балакирев"
    views = 14356
    comments = 12
```

**Пояснение:**
Все данные являются **атрибутами класса**, то есть общими и хранятся в пространстве имён `DataBase`.

---

### Задача 2

**Тренируем:**

* изменение атрибутов класса
* динамическое добавление атрибутов

**Условие:**
Объявите класс `Goods` со следующими атрибутами:

* `title`: `"Мороженое"`
* `weight`: 154
* `tp`: `"Еда"`
* `price`: 1024

После объявления класса:

1. измените значение атрибута `price` на `2048`;
2. добавьте атрибут `inflation` со значением `100`.

#### Решение:

```python
class Goods:
    title = "Мороженое"
    weight = 154
    tp = "Еда"
    price = 1024

Goods.price = 2048
Goods.inflation = 100
```

**Пояснение:**
Изменения происходят **в классе**, поэтому они будут видны всем объектам этого класса.

---

## 🟡 Средний уровень

### Задача 3

**Тренируем:**

* атрибуты класса и объекта
* счётчик объектов
* различие между общими и локальными данными

**Условие:**
Объявите класс `TravelBlog` с атрибутом класса:

* `total_blogs = 0`

Создайте объект `tb1` и добавьте ему локальные свойства:

* `name`: `'Франция'`
* `days`: 6

После этого увеличьте `total_blogs` на 1.

Создайте объект `tb2` и добавьте ему локальные свойства:

* `name`: `'Италия'`
* `days`: 5

Снова увеличьте `total_blogs` на 1.

#### Решение:

```python
class TravelBlog:
    total_blogs = 0

tb1 = TravelBlog()
tb1.name = 'Франция'
tb1.days = 6
TravelBlog.total_blogs += 1

tb2 = TravelBlog()
tb2.name = 'Италия'
tb2.days = 5
TravelBlog.total_blogs += 1
```

**Пояснение:**

* `total_blogs` — общий счётчик для всех объектов;
* `name` и `days` — индивидуальные данные конкретного блога.

---

### Задача 4

**Тренируем:**

* проверку наличия атрибутов
* понимание локальных и классовых свойств

**Условие:**
Объявите класс `Person` с атрибутами:

* `name`: `'Боб Бобов'`
* `job`: `'Программист'`
* `city`: `'Нальчик'`

Создайте объект `p1` этого класса.

Проверьте, существует ли у объекта `p1` **локальный** атрибут `job`.
Выведите `True`, если он существует именно в объекте `p1`, и `False` — если нет.

#### Решение:

```python
class Person:
    name = 'Боб Бобов'
    job = 'Программист'
    city = 'Нальчик'

p1 = Person()

result = 'job' in p1.__dict__
print(result)
```

**Пояснение:**
Атрибут `job` существует **в классе**, но не в объекте.
Поэтому результат будет `False`.

---

## 🔴 Сложный уровень

### Задача 5

**Тренируем:**

* порядок поиска атрибутов
* перекрытие атрибутов класса атрибутами объекта
* работу с `__dict__`

**Условие:**
Объявите класс `Device` с атрибутами:

* `brand = "Generic"`
* `power = 100`

Создайте объект `d1`.
Затем:

1. измените `power` **у объекта** `d1` на `300`;
2. удалите атрибут `power` у объекта `d1`;
3. выведите значение `d1.power`.

Объясните, почему получилось именно такое значение.

#### Решение:

```python
class Device:
    brand = "Generic"
    power = 100

d1 = Device()
d1.power = 300
del d1.power

print(d1.power)
```

**Пояснение:**
После удаления локального атрибута `power` Python не находит его в объекте и берёт значение из класса.
Результат: `100`.

---

### Задача 6

**Тренируем:**

* массовое создание объектов
* различие классовых и объектных атрибутов
* работу с циклами и условиями

**Условие:**
Объявите класс `Student` с атрибутом класса:

* `school = "Python School"`

Создайте список из трёх объектов этого класса.
Каждому объекту добавьте локальный атрибут `name` (разные имена).

Затем:

1. измените `school` в классе на `"Advanced Python School"`;
2. выведите для каждого объекта его `name` и `school`.

#### Решение:

```python
class Student:
    school = "Python School"

students = []

names = ["Алексей", "Мария", "Игорь"]

for name in names:
    s = Student()
    s.name = name
    students.append(s)

Student.school = "Advanced Python School"

for s in students:
    print(s.name, "-", s.school)
```

**Пояснение:**

* `name` — локальное свойство каждого объекта;
* `school` — атрибут класса, общий для всех студентов.

---

## Итог

После выполнения этих задач студент должен:

* уверенно различать атрибуты класса и объекта;
* понимать механизм поиска атрибутов;
* осознавать последствия изменения и удаления свойств;
* быть готовым к следующему уроку — **методам и параметру `self`**.

Если нужно, в следующем шаге могу:

* оформить эти задачи в формате **проверочной работы**;
* добавить **варианты с ловушками** для продвинутых студентов;
* подготовить **автотесты** под эти задания.
